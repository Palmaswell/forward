export enum Headline {
  primary = 'primary',
  secondary = 'secondary'
}

export enum HeadlineOrder {
  h1 = 'h1',
  h2 = 'h2',
  h3 = 'h3'
}

export enum Layout {
  default = 'default',
  custom = 'custom',
}
