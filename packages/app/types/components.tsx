
export enum TransitionStatus {
  entering = 'entering',
  exiting = 'exiting',
  entered = 'entered',
  exited = 'exited'
}

export enum Headline {
  primary = 'primary',
  secondary = 'secondary'
}

export enum HeadlineOrder {
  h1 = 'h1',
  h2 = 'h2',
  h3 = 'h3'
}

export enum Layout {
  default = 'default',
  custom = 'custom',
}

export enum CopyTag {
  div = 'div',
  p = 'p'
}

export enum Topbar {
  primary = 'primary',
  secondary = 'secondary'
}
